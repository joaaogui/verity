# Google Gemini API key (required for validation / extraction / suggestions)
GEMINI_API_KEY=

# Basic auth for /tests (format: username:password).
# Required in production — without it, /tests returns 404.
# In development, /tests stays open when unset.
TESTS_BASIC_AUTH=
